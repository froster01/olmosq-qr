# Claude Code Instructions

Use `AGENTS.md` as the shared source of truth for this repository.

Claude-specific reminders:

- Read `AGENTS.md` before editing.
- Follow the mandatory Next.js 16 rule in `AGENTS.md`: read the relevant local docs in `node_modules/next/dist/docs/` before touching Next.js routing, App Router APIs, Server Components, Server Functions, Route Handlers, caching, metadata, or config.
- Keep changes scoped, protect existing user work, and run the relevant verification command before reporting success.

@AGENTS.md

